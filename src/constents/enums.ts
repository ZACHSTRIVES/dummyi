export enum ColorMode {
    DARK = "dark",
    LIGHT = "light",
}

export enum Locales {
    EN = "en",
    ZH = "zh"
}

export enum ExportType {
    CSV = "csv",
    TSV = "tsv",
    JSON = "json",
    XML = "xml",
    // SQL = "sql",
    // HTML = "html",
    // Markdown = "markdown",
    // CSHARP = "csharp",
    // JAVA = "java",
}


